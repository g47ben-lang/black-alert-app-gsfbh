<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.BlackAlert" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:fontFamily">@font/ba_font</item>
        <item name="fontFamily">@font/ba_font</item>
    </style>

    <style name="Theme.BlackAlert.Bar" parent="Theme.Material3.Dark">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorSurface">@color/surface</item>
        <item name="android:fontFamily">@font/ba_font</item>
        <item name="fontFamily">@font/ba_font</item>
    </style>

    <!-- מסך ההתראה — מסך מלא -->
    <style name="Theme.BlackAlert.Alert" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:fontFamily">@font/ba_font</item>
        <item name="fontFamily">@font/ba_font</item>
    </style>
</resources>
